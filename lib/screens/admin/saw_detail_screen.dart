import 'package:flutter/material.dart';

class SawDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail SAW'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Halaman Detail SAW'),
      ),
    );
  }
}
